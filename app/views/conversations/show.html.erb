<% provide(:title, @conversation.id) %>
<!--   <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= InformationUser.where(user_id: @conversation.recipient_id).pluck :first_name %>
      </h1>
    </section>
  </aside> -->
<div class="row col-md-8 col-md-offset-2">
  <h1><%= @conversation.messages %></h1>

<div class="panel panel-default">
  <% if @conversation.messages.any? %>
    <div class="panel-body" id="messages">
      <%= render partial: 'messages/message', collection: @conversation.messages%>
    </div>
  <%else%>
    <div class="panel-body hidden" id="messages">
    </div>
  <%end%>
</div>

  <%= render partial: 'messages/message_form', locals: {message: @message, conversation: @conversation}%>
</div>
